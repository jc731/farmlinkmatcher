# Supabase (required for app and migrations)
# Copy to .env and fill in values from your Supabase project settings.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Optional: for server-side admin operations (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=
